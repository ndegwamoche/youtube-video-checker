import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Render the App component inside the WordPress admin container
ReactDOM.render(<App />, document.getElementById("youtube-video-checker"));
